#if文
条件分岐はif文を使う。

```php
// if
if(条件式){
  処理
}

// if ~ else
if(条件式){
  真のときの処理
}
ele {
  偽のときの処理
}

// if ~ elseif ~ else
if(条件式){
  処理
} elseif(条件式) {
  処理 
} else {
  処理
} 

条件には以下のような常にtrue,falseになる書き方ができる
if (true) or (1)     // 常にtrue
if (false) or (0)    // 常にfalse
```